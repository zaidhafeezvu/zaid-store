/**
 *
 * WishList Reducer
 *
 */

const initialState = {
  items: []
};

const wishListReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default wishListReducer;