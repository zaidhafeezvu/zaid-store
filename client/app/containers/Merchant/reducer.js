/**
 *
 * Merchant Reducer
 *
 */

const initialState = {};

const merchantReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default merchantReducer;