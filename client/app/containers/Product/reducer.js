/**
 *
 * Product Reducer
 *
 */

const initialState = {
  products: [],
  selectedProduct: null
};

const productReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productReducer;