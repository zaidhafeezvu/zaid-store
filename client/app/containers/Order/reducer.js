/**
 *
 * Order Reducer
 *
 */

const initialState = {
  orders: [],
  selectedOrder: null
};

const orderReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default orderReducer;