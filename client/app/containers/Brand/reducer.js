/**
 *
 * Brand Reducer
 *
 */

const initialState = {};

const brandReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default brandReducer;