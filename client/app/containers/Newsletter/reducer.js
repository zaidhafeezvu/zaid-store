/**
 *
 * Newsletter Reducer
 *
 */

const initialState = {};

const newsletterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default newsletterReducer;